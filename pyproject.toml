[project]
name = "kapruka-gift-concierge"
version = "0.1.0"
description = "Kapruka Gift Concierge Project"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "."}

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]